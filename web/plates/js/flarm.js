angular.module('appControllers').controller('FlarmCtrl', FlarmCtrl); // get the main module contollers set
FlarmCtrl.$inject = ['$rootScope', '$scope', '$state', '$http', '$interval']; // Inject my dependencies

function FlarmCtrl($rootScope, $scope, $state, $http, $interval) {
}