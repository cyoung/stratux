dependencies:
  pre:
    - sudo apt-get update; sudo apt-get install libusb-dev; cd /tmp; git clone git://git.osmocom.org/rtl-sdr.git; cd rtl-sdr; mkdir build; cd build; cmake ../; make; sudo make install; sudo ldconfig
  override:
    - make
test:
  override:
    - make test
